# ClaudeVN Compute Engine Requirements

# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# LLM providers
openai>=1.3.0
# tiktoken>=0.5.0  # Optional: uncomment for accurate token counting (requires Rust)
# anthropic>=0.7.0  # Phase 2

# Claude Agent SDK (replaces subprocess-based Claude CLI spawning)
claude-agent-sdk>=0.1.37

# HTTP client
httpx>=0.25.0
aiohttp>=3.9.0

# Utilities
python-json-logger>=2.0.0
psutil>=5.9.0

# Shared library (local development)
# Note: For Docker builds, shared library is installed separately via Dockerfile
# For local development, use: pip install -e ../shared
# -e ../shared

