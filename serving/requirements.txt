# ClaudeVN Serving Component Requirements

# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# File handling
aiofiles>=23.2.0

# HTTP client
httpx>=0.25.0

# LLM providers
anthropic>=0.40.0

# Redis (optional, for distributed setups)
redis>=5.0.0
aioredis>=2.0.1

# Utilities
python-json-logger>=2.0.0
pyyaml>=6.0.0

# Shared library (local development)
# Note: For Docker builds, shared library is installed separately via Dockerfile
# For local development, use: pip install -e ../shared
# -e ../shared

